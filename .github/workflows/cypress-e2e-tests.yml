name: E2E Tests
on: push
  # pull_request:
  #   branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout
          uses: actions/checkout@v4
        - uses: actions/setup-node@v3
          with:
            node-version: 20
            run: npm buid
            start : npm start
        - name: Install
          run: npm install
        - name : Start 
          run: npm run wdio
       